  
  //Login Paths
  export enum LoginPaths {
    login = "login",
    getNewToken = "auth/newToken",
  }

  export enum UserPaths {
    basePath = "usuarios",
    get = UserPaths.basePath,
    post = UserPaths.basePath,
    put = UserPaths.basePath,
    delete = UserPaths.basePath,
  }

  export enum ProductosPaths {
    basePath = "productos",
    get = ProductosPaths.basePath,
    post = ProductosPaths.basePath,
    put = ProductosPaths.basePath,
    delete = ProductosPaths.basePath,
  }

  export enum GuarnicionPaths {
    basePath = "guarniciones",
    get = GuarnicionPaths.basePath,
    post = GuarnicionPaths.basePath,
    put = GuarnicionPaths.basePath,
    delete = GuarnicionPaths.basePath,
  }

  export enum CombosnPaths {
    basePath = "combos",
    get = CombosnPaths.basePath,
    post = CombosnPaths.basePath,
    put = CombosnPaths.basePath,
    delete = CombosnPaths.basePath,
  }





  
